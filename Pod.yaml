---
apiVersion: v1
kind: Pod
metadata:
  name: mikey-pod
  labels:
    app: nginx
spec:
  containers:
    - name: mikey-cont
      image: alpine
      args:
        - sleepy
      command:
        - 30s
  restartPolicy: Never
     